export class Skill {
    constructor(name, description) {
        this.name = name;
        this.description = description;
    }

    action({
        constructor,
        screenElements,
        screenWidth,
        screenHeight,
        speed,
        atGameStart
    }) {  }
}